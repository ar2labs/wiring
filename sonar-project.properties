sonar.projectKey=com.github.ar2labs-wiring
sonar.projectName=Wiring
sonar.projectVersion=1.0-SNAPSHOT

# =====================================================
#   Properties that will be shared amongst all modules
# =====================================================

# SQ standard properties
sonar.sources=src
sonar.tests=tests

sonar.language=php
sonar.sourceEncoding=UTF-8

# For PHP (the plugin currently supports only absolute path in the reports, so this test project is not portable)
sonar.php.coverage.reportPath=coverage-report.clover
sonar.php.tests.reportPath=test-report.xml
